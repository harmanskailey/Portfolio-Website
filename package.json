{
  "name": "harman-kailey-portfolio",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:og": "node scripts/build-og-image.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test:auth": "npm run build && node --experimental-strip-types scripts/test-auth.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "sanitize:images": "node scripts/sanitize-images.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@vercel/functions": "^3.7.5",
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/vercel": "^11.0.2",
    "prettier": "^3.9.5",
    "prettier-plugin-astro": "^0.14.1",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "path-to-regexp": "6.3.0"
  }
}
